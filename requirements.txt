python-dotenv
b2sdk
pytest
truststore
ruff
pydantic
fastapi
uvicorn[standard]
sqlalchemy
genblaze-openai
genblaze-s3
# Backlog 3.2. Note: genblaze-google ships ImagenProvider, which cannot serve
# nano-banana (Imagen API + no image inputs) — see nova/providers/gemini_image.py.
# It's still pulled in for the Veo video stage (backlog 6.1/6.2), and google-genai
# is the client Nova's own nano-banana adapter calls.
genblaze-google
google-genai
# Animatic stage (backlog 6.1/6.2): Runway primary, Luma fallback, GMI draft
# tier (also the Flux image fallback for 3.3).
genblaze-runway
genblaze-luma
genblaze-gmicloud
# Audio stage (backlog 6.3): ElevenLabs SFX fallback; Stability primary is a
# Nova adapter calling the REST API directly via requests.
genblaze-elevenlabs
requests
# Assembly (backlog 7.1): bundled ffmpeg fallback when none is on PATH.
imageio-ffmpeg
